type: object
additionalProperties: false
properties:
  webp:
    type: object
  pngquant:
    type: object
  jpegtran:
    type: object
  webpFileName:
    type: string
  fallbackFileName:
    type: string
  inputMime:
    type: string
  export:
    enum: [dom, fallbackPath, paths]
  alt:
    anyOf:
      - type: string
      - type: "null"
  publicPath:
    anyOf:
      - type: string
      - type: "null"